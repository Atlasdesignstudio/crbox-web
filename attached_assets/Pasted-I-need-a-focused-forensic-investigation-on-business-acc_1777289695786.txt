I need a focused forensic investigation on business account registration only.

Do not touch the personal account flow. Personal signup is working and should remain untouched unless you find shared code that is unquestionably breaking business registration too. The goal here is to understand exactly why business account creation is failing and fix only that path.

## Objective
Investigate, isolate, and fix the business signup flow so that it works end-to-end in the real browser UI, with the correct backend payload and successful registration lifecycle.

## Important context
- Personal account signup is working.
- Business account signup is not working in the browser UI.
- I want a deep investigation first, not random UI edits.
- Do not redesign the business flow further until the root cause is clearly identified.
- If the issue is in shared code, document it carefully and make the smallest safe fix possible.

## What I need you to do
### 1. Reproduce the failure in the real browser path
Use the actual browser UI flow for business account signup and reproduce the error.
I want the full real-user path, not a server-side shortcut and not a direct script that bypasses browser behavior.

Capture:
- all network calls triggered on submit
- request payload sent to registration
- response body
- whether /crbox-svc-token succeeds
- whether postregisteruser is actually reached
- whether the failure happens before submit, at token retrieval, at registration, or after registration

### 2. Compare business flow against what the backend expects
Audit the current business payload field-by-field and compare it against the known working legacy/current behavior and the confirmed backend contract we already understand.

Specifically inspect:
- Consignee.IsCompany
- Consignee.IdentificationType
- Consignee.IdentificationNumber
- Consignee.ConsigneeName
- Consignee.ConsigneeLastName1
- Consignee.ConsigneeLastName2
- Consignee.ContactName1
- Consignee.ContactName2
- Consignee.AlternativeEmail
- Consignee.BirthDate
- Consignee.ResidenceCountry
- Consignee.Sucursal.IdSucursal
- Phones
- Addresses
- CompanyCode
- address INEC mapping
- domicilio branch vs sucursal branch behavior

I need you to confirm whether the current business payload is:
- fully correct
- partially wrong
- structurally accepted but semantically rejected
- or inconsistent with what the backend/legacy working flow expects

### 3. Verify whether the failure is caused by:
Check and explicitly rule in or out each of the following:

- wrong or missing field values
- wrong business-specific mapping
- duplicate company-name restriction
- duplicate email
- duplicate identification number
- invalid IdentificationType
- incorrect IsCompany value
- address cascade producing wrong values
- hidden field mismatch
- business-only UI state bug
- submit handler bug
- shared registration bug that only surfaces in business mode
- rate limit / timing issue
- validation/UI bug where the user sees a generic error but the backend returned something more precise

### 4. Inspect the current browser-visible error handling
I want you to trace exactly why the user ends up seeing:

“Ocurrió un error al procesar el registro. Intente de nuevo.”

Determine:
- what raw backend message is actually coming back
- whether that message is being swallowed by generic frontend error handling
- whether business registration has a more specific backend response that we are not surfacing
- whether the current UI should expose a more accurate error state for business signup

### 5. Fix only what is necessary
Once the root cause is identified, make the minimum safe fix needed to get business registration working.

Rules:
- do not touch personal signup unless absolutely necessary
- do not change backend contracts
- do not introduce speculative fields
- do not redesign the UI as part of this debugging pass
- preserve the current structure unless the failure itself requires markup or JS correction

### 6. Validate the full business lifecycle after the fix
After fixing it, run the full browser-level validation for business account:

- registration succeeds
- login succeeds
- getuserinfo succeeds
- business account is recognized correctly
- dashboard loads
- Mi Cuenta loads
- account type is coherent
- branch / domicilio data is coherent
- no broken auth/session behavior

If possible, also confirm whether the old/current portal recognizes that new business account correctly.

## Deliverables I want back
When you report back, give me this in a structured way:

1. Root cause
   - what exactly was wrong
   - where it was wrong
   - why it affected business but not personal

2. Evidence
   - relevant request payload
   - relevant response body
   - relevant frontend code path
   - relevant backend-facing mismatch if any

3. Fix applied
   - exact file(s)
   - exact logic changed
   - why this is the minimum safe fix

4. Validation result
   - business registration
   - login
   - getuserinfo
   - dashboard
   - Mi Cuenta

5. Safety check
   - confirm whether personal signup was untouched
   - or, if shared code had to be modified, explain why personal flow remains safe

## Very important
This is a forensic debugging task, not a redesign task.

Do not respond with “I think” or “likely”.
I want:
- reproduction
- evidence
- diagnosis
- fix
- validation

Focus only on making business account signup work 100% in the real browser flow.