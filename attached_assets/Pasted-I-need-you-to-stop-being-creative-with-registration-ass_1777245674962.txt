I need you to stop being creative with registration assumptions and align both signup flows strictly to the backend behavior that has already been validated.

This is a technical contract-correction task first, and only secondarily a UX task.

## Core instruction
Do not invent cleaner semantics, nicer mappings, or simplified interpretations unless they are proven safe against the real backend lifecycle.

The signup system must behave in a way that is boring, predictable, and backend-correct.

That means:

- same backend endpoints that are confirmed working
- same payload families that are confirmed working
- same lifecycle expectations that are confirmed working
- only modernize the UI around that truth

If there is any tension between a “nicer” frontend idea and what the backend reliably accepts, choose the backend-safe implementation.

---

## What is already confirmed and must be respected

### Registration lifecycle baseline
A working registration lifecycle has already been validated against the real system.

That means the implementation must preserve a flow that can successfully do all of this:

1. POST https://clients.crbox.cr/api/crboxwebapi/postregisteruser
2. POST https://clients.crbox.cr/authtoken
3. GET https://clients.crbox.cr/api/crboxwebapi/getuserinfo/{email}
4. successful portal bootstrap afterward
5. successful postedituser afterward

Do not route registration to staging.  
Do not change this backend family.  
Do not introduce alternate speculative flows.

### Auth behavior
The normal login flow is already working and must remain untouched.

The only allowed login-related behavior in this work is:
- an additive post-registration CRBOXAuth.doLogin(email, password) attempt

Do not redesign, refactor, or alter login.html or the standard login behavior.

---

## Personal account: technical requirements
The personal flow must continue to produce a payload that is semantically aligned with the confirmed working lifecycle.

At minimum, the backend-safe personal registration payload must include correctly mapped values for:

- Consignee.ConsigneeName
- Consignee.ConsigneeLastName1
- Consignee.ConsigneeLastName2
- Consignee.Email
- ConfirmEmail
- Password
- ConfirmPassword
- Consignee.IdentificationType
- Consignee.IdentificationNumber
- Consignee.BirthDate
- Consignee.IsCompany = '0'
- Consignee.ResidenceCountry = 'CR'
- Consignee.Sucursal.IdSucursal
- Phones
- Addresses

Also:
- Consignee.AlternativeEmail = ''
- Consignee.ContactName1 = ''
- Consignee.ContactName2 = ''
- Consignee.IdResponsabilidad = ''
- Consignee.Responsabilidad = '0'
- CompanyCode optional if provided

### Personal-specific rules
- Birth date must be collected and sent.
- ID type and ID number must be collected and sent unless a new real lifecycle test proves deferral is safe.
- Full name splitting must remain backend-safe:
  - token 1 → ConsigneeName
  - token 2 → ConsigneeLastName1
  - token 3+ → ConsigneeLastName2
- Single-token names must remain blocked before submit.
- Phone must be sent as a real structured Phones array.
- Address must be sent as a real structured Addresses array.
- Sucursal and domicilio must both produce backend-valid address payloads.

---

## Empresarial account: technical requirements
The business flow must not remain a fake contact CTA. It has already been validated on the real old/live system as a true self-serve registration mode.

The new-site business implementation must preserve that backend-compatible behavior.

At minimum, the backend-safe business payload must include correctly mapped values for:

- Consignee.ConsigneeName = business/company name
- Consignee.ConsigneeLastName1 = ''
- Consignee.ConsigneeLastName2 = ''
- Consignee.Email
- ConfirmEmail
- Password
- ConfirmPassword
- Consignee.IdentificationType = 'Otro'
- Consignee.IdentificationNumber
- Consignee.IsCompany = '1'
- Consignee.ResidenceCountry = 'CR'
- Consignee.Sucursal.IdSucursal
- Phones
- Addresses
- Consignee.ContactName1
- Consignee.ContactName2
- Consignee.AlternativeEmail
- CompanyCode optional
- Consignee.BirthDate = ''

### Empresarial-specific rules
- Do not force personal-name semantics onto company mode.
- Do not require birth date in business mode unless a new direct test proves it is needed.
- IdentificationType in business mode must stay backend-safe and should remain "Otro" unless a real validated lifecycle test proves otherwise.
- IsCompany must be set correctly and explicitly.
- Business mode still needs real phone, address, sucursal, email, password, and identification data.
- Company code must remain optional unless a real validated test proves otherwise.

### Critical warning for empresarial
Do not get “clean” or “semantic” with business contact mapping unless validated.
The backend may historically tolerate odd field usage.  
If you changed business contact semantics in a way that is prettier but less backend-compatible, revert toward the safer proven mapping.

Pay special attention to:
- Consignee.ContactName1
- Consignee.ContactName2
- Consignee.AlternativeEmail
- surname blanking
- Consignee.BirthDate
- Consignee.IdentificationType
- Consignee.IsCompany

If there is any doubt, compare the current business payload against the previously validated working business payload and bias toward the backend-safe version.

---

## Address handling: technical requirements
This is an area where creativity is dangerous. Keep it backend-correct.

### Sucursal mode
For:
- Sabana Norte
- Guadalupe
- Guachipelín-Escazú

You may use prefill, but only if it produces a real valid address payload with correct INEC-derived values.

That means:
- province resolved correctly
- canton resolved correctly
- district resolved correctly
- Consignee.Sucursal.IdSucursal correct
- address object structurally valid

Do not guess codes.  
Do not approximate district/canton/province values.  
Do not send fake placeholders.

### Domicilio mode
Domicilio must continue to require:
- province
- canton
- district
- detailed address text

If resolution fails for province, canton, or district:
- block submission
- do not silently send 0

### General address rules
- stale domicilio values must be cleared when switching to sucursal
- Addresses must always be a real structured array
- Phones must always be a real structured array
- barrio handling must stay consistent with what the backend tolerates; do not invent a new scheme

---

## Endpoint rules
These must remain exactly aligned to the working lifecycle:

- Registration: https://clients.crbox.cr/api/crboxwebapi/postregisteruser
- Login: https://clients.crbox.cr/authtoken
- Profile bootstrap: https://clients.crbox.cr/api/crboxwebapi/getuserinfo/{email}

Do not point registration to test.clients.crbox.cr.  
Do not create alternate registration endpoints.  
Do not introduce speculative backend paths.

---

## Validation rules
You must validate with real accepted email domains.  
Do not use throwaway domains like @mailinator.com to judge whether registration is broken.

For both personal and business, the final implementation should only be considered correct if a real test confirms:

- registration success
- login success
- getuserinfo coherence
- dashboard bootstrap
- Mi Cuenta bootstrap
- postedituser success if possible

If any of those fail, treat it as a real lifecycle issue, not as “good enough because the UI looks right.”

---

## UX rules
You may modernize the UX, but only around backend truth.

Allowed:
- better stepper
- cleaner grouping
- less visual density
- better mobile layout
- clearer labels
- stronger hierarchy
- guided setup mode in Mi Cuenta

Not allowed:
- inventing perks
- inventing benefits for business accounts
- removing fields just to make the form feel lighter if the lifecycle needs them
- changing payload semantics just because a different mapping feels cleaner

The UX must serve the backend-correct flow, not redefine it.

---

## What I need you to ensure
Please make sure all of the following are true:

1. Cuenta Personal
   - submits a backend-correct payload
   - still creates a stable account
   - still logs in correctly
   - still bootstraps the portal correctly

2. Cuenta Empresarial
   - is a real working registration mode, not a contact-only shortcut
   - submits a backend-correct payload
   - preserves the business-specific semantics that the backend actually accepts
   - still creates a stable account
   - still logs in correctly
   - still bootstraps the portal correctly

3. No unnecessary creativity
   - no speculative remapping
   - no field semantics invented for cleanliness
   - no backend contract drift hidden behind a nicer UI

---

## Deliverable
I want you to:
- make the implementation technically correct against the real backend behavior
- verify that both personal and empresarial flows still work end-to-end
- correct any field mapping that became too creative or risky
- only then keep/refine the modern UX around that safe foundation

The priority order is:

backend correctness → lifecycle stability → UX polish

Not the other way around.