The Mis Facturas refinement appears incomplete because the page still is not rendering real bill rows, even though the task was meant to bind the page strictly to the confirmed { Factura, Recibos[] } response shape.

Please treat this as a debug + completion continuation, not a new redesign task.

## Problem
The bills page UI still shows an empty state / zero-state behavior instead of rendering the real invoices.

That means one of these is still wrong:
- the fetch is not actually being called with the correct authenticated email/date range
- the response is being received but mapped incorrectly
- the mapped bill array is empty because the mapper is dropping rows
- the renderer is still expecting the wrong shape
- the search/date defaults are filtering everything out incorrectly
- the page is rendering the empty state before/after data incorrectly
- a JS error or silent guard is preventing row render
- the summary/table state logic is not consuming the same final mapped dataset

## What to do now
Perform an end-to-end debug of Mis Facturas only and fix it until real rows render.

### 1. Verify the actual runtime data flow
Check, in order:

1. Is the bills request actually firing on page load?
2. What exact URL is being requested?
3. Which email is being used?
4. Which start/end dates are being used by default?
5. Does the API return 200?
6. Is the returned array non-empty?
7. How many rows come back raw?
8. How many rows survive mapping?
9. How many rows reach the renderer?
10. Does the renderer receive the expected mapped shape?

Do not assume. Verify each stage in code.

### 2. Log and isolate the failure point
Temporarily add clear debug logs for:
- raw getBills() response length
- mapped bills length
- first raw bill object
- first mapped bill object
- final array passed into the table renderer
- any date values used for filtering
- whether the table chooses empty-state vs render-state and why

Then identify the exact stage where valid rows are getting lost.

### 3. Re-check the mapper against the real shape
The bills page must map from the confirmed real shape:

- top-level array
- each row = { Factura, Recibos[] }

Re-verify that the mapper is correctly extracting at minimum:
- Factura.factura
- Factura.masterAirShipment.masterairshipmentnumber
- Factura.weigth
- Factura.volumentricWeigth
- Factura.cantidadBultos
- Factura.total
- Factura.descuentoCorporativo._nombre
- Factura.billedDate
- fallback Factura.createdDate
- Recibos[]

Make sure rows are not being dropped just because:
- billedDate is null
- createdDate is null
- Recibos is empty
- descuentoCorporativo is null-ish
- masterAirShipment is partially null

A row with partial nulls should still render if the invoice exists.

### 4. Re-check default filtering logic
Audit whether the current default date range or search logic is accidentally filtering out real bills.

Specifically verify:
- whether the default date range is too narrow
- whether the date formatting sent to the API is correct
- whether the page is applying any additional client-side filtering after fetch
- whether the search input defaults to a non-empty or malformed value
- whether the renderer only shows rows after a button click instead of initial load

If the API already returns the correct filtered set, do not re-filter it incorrectly on the client.

### 5. Re-check empty-state logic
Make sure the empty state only renders when the final mapped bill dataset is truly empty.

Do not show empty state when:
- data is still loading
- mapping failed silently
- rendering errored
- one helper returned null
- stats and table are reading different arrays

The stats cards, table rows, and empty state must all consume the same final mapped bill array.

### 6. Confirm table renderer is bound to mapped rows only
The row renderer, summary cards, expansion/detail area, and empty state must all consume the mapped object shape only.

No lingering raw access.
No flat guessed invoice shape.
No stale helper expecting old field names.

### 7. Finish only when real rows display
This is not done until:
- real bill rows visibly render in the table
- total count reflects returned rows
- total billed reflects real totals if supported
- empty state only appears when the API actually returns no bills
- no fake payment status is introduced

## Deliverable
Reply back with:
1. the exact root cause
2. what stage was dropping the rows
3. what was fixed
4. confirmation that real invoices now render from the confirmed { Factura, Recibos[] } shape
5. any remaining limitation caused by backend semantics rather than frontend implementation

This is a functional completion/debug pass. Do not redesign the page. Do not change truthfulness rules. Fix the real render path end to end.