We need a small frontend consistency fix pass on the signup flow.

This is not a backend task and not a registration-logic rewrite.  
The current flow is working, but there are still 2 visible frontend issues that need to be corrected properly.

## Scope
Only fix these two things:

1. Delivery-card right-side selector vertical misalignment
2. Business cédula jurídica frontend validation inconsistency

Do not touch:
- backend logic
- payload structure
- registration flow architecture
- personal/business branching logic
- styling outside of what is needed for these 2 fixes

---

## Issue 1 — Delivery-card selector is still visually misaligned
The right-side selector on the delivery cards is still sitting too high in the row, especially in the selected state.

This is happening in the “¿Dónde retiran sus paquetes?” cards, and it affects the visual quality of the component.

The previous fix grouped the badge + selector, but the real alignment issue is still present in rendering.

### What to do
Please inspect the actual rendered card and fix the layout so that the full right-side group:
- badge
- selected check / unselected radio circle

is visually and mathematically centered against the full card height.

### Requirements
- fix this in both personal and business flows
- selected and unselected states must align equally well
- do not redesign the cards
- do not change copy/content
- do not just add another random centering patch
- actually fix the row structure so the right-side column is centered correctly inside the full card

If needed, refactor the internal flex/grid composition of the delivery card so the right side behaves like a proper third column.

---

## Issue 2 — Business cédula jurídica validation is not behaving correctly in the UI
The business cédula jurídica field says it expects 10 numeric digits, but in the current UI it still visually accepts values longer than 10 digits (and likely shorter ones too) without clearly reacting.

So the frontend experience is inconsistent:
- helper text says one thing
- field behavior suggests another

### What to do
Make the business cédula jurídica field behave consistently in the UI.

### Required behavior
- only the business cédula jurídica field should be affected
- the expected business rule remains:
  - exactly 10 digits
  - numeric
- if the user types hyphens, normalize them out
- the field should not appear valid if the value is not exactly 10 digits

### UX requirement
Please improve the UX consistency, not just submit-time behavior.

That means:
- validate clearly at the right interaction point
- show the inline error message reliably
- make sure the field does not sit in an apparently valid visual state when it contains too many or too few digits

If appropriate, you may also improve the input behavior itself so that the user cannot freely keep entering obviously invalid length without feedback.

### Important
- do not touch personal validation
- do not add future alphanumeric support
- do not change backend contract
- do not rewrite the form flow

---

## Deliverables
When you report back, include:

1. exactly how the delivery-card selector alignment was fixed
2. confirmation that it now renders correctly in both personal and business flows
3. exactly how the cédula jurídica UI validation was corrected
4. confirmation that it now behaves consistently with the “10 dígitos numéricos” requirement
5. confirmation that no backend or broader registration logic was changed

This is a frontend visual/validation consistency fix only.