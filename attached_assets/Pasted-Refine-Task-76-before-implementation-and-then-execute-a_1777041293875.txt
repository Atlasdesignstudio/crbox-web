Refine Task #76 before implementation and then execute against the refined version.

The direction of Task #76 is correct, but I want it tightened so the portal truthfulness pass is explicit, strict, and does not leave room for fake inferred UI. Do not just restate the task. Improve it using the requirements below, then implement the refined version.

# Core principle
Across the CRBOX client portal, every visible module must be one of these:
1. real backend data,
2. truthfully derived from real backend data,
or
3. honestly unavailable / neutral / próximamente.

No fake metrics.
No fake activity feed.
No fake progress bars.
No fake preferences pretending to save.
No fake operational alerts.
No fake package or bill content.

# Refine the task with these additions

## 1. Be stricter about derivation vs fabrication
For any widget that is “derived,” the derivation must be defendable from actual backend fields already returned by the confirmed endpoints.

That means:
- do not infer operational meaning that is not actually present in the payload
- do not fabricate timeline events from vague dates
- do not derive “pending invoices” unless there is a real bill-state signal
- do not derive “consolidated packages” unless there is a real field or reliable grouping logic in the package response

If a metric cannot be defensibly computed, it must be neutralized rather than guessed.

## 2. Explicitly treat getuserinfo as the canonical profile source
The refined task should explicitly state that:
- getuserinfo is the canonical bootstrap for authenticated user/profile state
- any profile-related UI should be refreshed from it after successful profile save
- all IDs used in postedituser must come from it
- if profile cache exists, it must be cleared after successful postedituser

## 3. Make dashboard truthfulness rules explicit
Refine the dashboard section so these rules are explicit:

### Dashboard identity block
- name
- casillero / locker number
- sucursal
must come only from getuserinfo
No hardcoded fallback names ever.

### Dashboard summary cards
For:
- Paquetes en camino
- Facturas pendientes
- Paquetes consolidados

each card must be classified as:
- real/derived
or
- unavailable/neutral

No card may display a fake number, fake progress, or fake percentage.

### Dashboard recent activity
Only build this if a real event list can be derived from actual package or bill data with meaningful dates and labels.
Otherwise show an honest empty state.
Do not create synthetic event rows just because the UI has a feed section.

## 4. Be explicit about Mis Paquetes screen
The refined task should state clearly:

- stats cards must come from the currently loaded real dataset
- status tab badges must be computed from that same real dataset
- list view and grid view must render from the exact same package array
- tracking modal must show real package data only
- detail modal must show real package data only
- edit button must remain disabled / próximamente if no real edit endpoint exists
- consolidation block must become clearly informational/próximamente if current backend does not support it truthfully
- informational blocks (“notificaciones”, “consejos”) must not be presented as live operational system events unless backed by real data

## 5. Be explicit about Mis Facturas screen
The refined task should state clearly:

- the bills table must be real
- [] is valid empty success
- summary cards must only display metrics actually computable from the real bills response
- payment info block must be clearly treated as static business content if not backend-driven
- do not present fake billing state labels if the response does not support them

## 6. Be explicit about Mi Cuenta preferences and delete-account
Refine this part to say:

- only preferences backed by real backend fields may remain interactive
- unsupported preferences must be visibly disabled or marked “Próximamente”
- no switch/toggle may appear to save if no backend support exists
- delete-account must not simulate a destructive flow if no real endpoint exists
- replace fake delete flow with support CTA or próximamente state

## 7. Tighten loading/empty/error behavior
The refined task must explicitly require:

- every API-driven module has a stable loading state
- [] is always treated as empty success, never as error
- one module failing must not crash the full page
- module-level failure states should be bounded and specific
- no flash of old hardcoded mock rows/cards before real data renders

## 8. Require codebase-wide fake-data audit
Add an explicit implementation requirement to search the codebase for:
- hardcoded portal stats
- hardcoded recent activity arrays
- placeholder rows/cards
- fake progress bars
- fake counts
- fake package cards
- fake bill rows
- fake preference persistence
and remove or neutralize them.

## 9. Require final reporting
At the end of implementation, report back with:
1. which modules are now fully real
2. which modules are derived from real data
3. which modules were neutralized because backend support is missing
4. why each neutralized module was not supportable yet
5. every file changed

# Confirmed backend/source-of-truth context to preserve in the refined task

- Login:
  POST https://clients.crbox.cr/authtoken
  returns access_token, token_type bearer, expires_in 86399

- Canonical profile bootstrap:
  GET https://clients.crbox.cr/api/crboxwebapi/getuserinfo/{email}

- Profile update:
  POST https://clients.crbox.cr/api/crboxwebapi/postedituser
  Bearer required
  production URL, not test
  success: {"StatusResult":"OK","Message":""}

- Packages:
  GET https://clients.crbox.cr/api/crboxwebapi/getuserpackages/{idConsignee}/{fecha_inicial}/{fecha_final}/{tracking}/{status}
  [] = valid empty success

- Bills:
  GET https://clients.crbox.cr/api/crboxwebapi/getfacturas/{email}/{fecha_inicial}/{fecha_final}
  [] = valid empty success

- Password recovery:
  GET https://clients.crbox.cr/api/crboxwebapi/getuserpasswordrecovery/{email}

- Direct browser fetch is valid
  CORS already confirmed
  proxy logic in server.py should be removed if it only existed due to earlier uncertainty

# Important non-redesign reminder
Do not redesign the portal.
Keep layout, styling, hierarchy, and visual language intact.
Only make the portal truthful and functional.

Now:
1. rewrite Task #76 with all refinements above,
2. then implement the refined version.