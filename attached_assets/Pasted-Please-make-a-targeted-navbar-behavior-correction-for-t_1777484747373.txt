Please make a targeted navbar behavior correction for the top-right account area, specifically for the difference between:

- the public site
- and the client portal

This is a logic + UX consistency fix, not a redesign.

## Context
Right now there are two different top-right behaviors being mixed in a confusing way:

1. Outside the portal (public site pages), it makes sense to have a top-right button that helps the logged-in user go to the portal/dashboard.
2. Inside the portal, once the user is already in the client portal, it does not make sense for normal users to still see a redundant top-right dashboard/admin-style CTA.

At the same time, there is one special exception:
- the test/admin account prueba@crbox.cr

For that specific account, the special admin shortcut behavior should remain.

So this needs to be implemented as a clear conditional rendering rule, not a global navbar change.

---

## Correct behavior I want

### A. Public site (outside the portal)
When the user is logged in and is on public pages, the top-right area can still show the access CTA that takes them into the portal/dashboard.

That behavior is fine.

---

### B. Inside the client portal — normal users
Once a normal user is already inside the portal, the top-right area should not show a redundant blue “Dashboard” or “Panel Admin” style CTA.

Instead, it should show the pattern that makes sense in a logged-in portal:

- a rectangular user/account chip
- user name visible
- dropdown affordance
- dropdown menu with actions such as:
  - Mi cuenta / Ver cuenta
  - Cerrar sesión
  - any other already-existing relevant account action if appropriate

The key point:
inside the portal, the user should feel like they are already in their account context — not being prompted again to go to “Dashboard”.

---

### C. Inside the client portal — special admin/test account
For the specific account:

- prueba@crbox.cr

the special behavior should remain exactly as intended:

- keep the blue “Panel Admin” button visible
- keep it positioned beside the user account area
- preserve the special admin access shortcut

This is the exception and should not be generalized to any other user.

---

## Critical rule
Please implement the logic explicitly like this:

### If email == prueba@crbox.cr
Keep the special admin/test rendering:
- show Panel Admin
- preserve current admin shortcut behavior
- keep the top-right area as intended for this one special account

### Else
For all other users:
- do not show the blue Panel Admin button inside the portal
- show the normal user/account dropdown behavior instead
- no redundant dashboard/admin CTA inside the portal

---

## Why this matters
The screenshot with the blue Panel Admin button is the admin/test account case.

That case is correct for prueba@crbox.cr.

The mistake would be letting that same pattern appear for every normal client account.

So the behavior must branch intentionally:
- special admin/test account → special admin UI remains
- every other user → normal account dropdown behavior

---

## What to inspect
Please inspect:

- current top-right navbar logic
- how nav-auth.js or equivalent navbar auth/render code decides what to render
- how portal pages differ from public pages
- whether the blue button is being injected globally without checking both:
  - whether the user is already inside the portal
  - whether the user is specifically prueba@crbox.cr
- whether the existing user-name dropdown markup/behavior already exists and just needs to be restored for portal pages for non-admin users

---

## What I want implemented

### Public pages
When logged in on public pages:
- keep the existing “go to dashboard/portal” behavior if that is the intended pattern

### Portal pages — normal users
Replace the redundant top-right dashboard/admin CTA with a proper user/account dropdown container.

It should:
- display the current user name
- look like an account chip/dropdown, not a CTA button
- open a small dropdown menu with relevant account actions
- feel coherent with the portal and existing CRBOX style

### Portal pages — prueba@crbox.cr
Preserve the current special admin/test experience:
- user account area still visible
- blue Panel Admin button still visible
- no regression to this admin shortcut

---

## UX intent
The navbar should communicate:

- outside portal → “go to your dashboard”
- inside portal, normal user → “you are already in your account; here are your account actions”
- inside portal, special admin/test account → “you are in the portal and also have admin access”

Right now that distinction is being blurred.

---

## Constraints
- do not redesign the whole navbar
- do not remove the logged-in identity display
- do not break admin/test account behavior
- do not break public-site login-to-portal access flow
- do not leak admin-only UI to normal users
- do not rely on vague heuristics; use the explicit special-account rule
- keep CRBOX visual language
- keep it simple and coherent

---

## Done looks like
- on public pages, logged-in users can still access the portal/dashboard appropriately
- inside the portal, normal users no longer see a redundant blue admin/dashboard-style button
- instead, normal users see a proper user/account dropdown
- the dropdown includes at least account-view and logout behavior
- inside the portal, prueba@crbox.cr still sees the blue Panel Admin button beside the user area
- admin/test account logic remains separated and intentional
- no regression across desktop or mobile

---

## Please report back with
1. what logic was causing the blue/admin-style button to appear incorrectly for normal users inside the portal
2. where the special-account condition is now defined
3. how you separated public-site behavior vs portal behavior
4. how the navbar rendering now differs for prueba@crbox.cr vs all other users
5. what dropdown/account menu behavior is now shown inside the portal for normal users
6. confirmation that the admin/test account keeps the blue Panel Admin button
7. confirmation that desktop and mobile were both reviewed visually