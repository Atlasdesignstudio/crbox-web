Please make a very targeted mobile spacing fix inside the client portal, specifically in Mis Paquetes and the related dashboard cards shown in the attached screenshots.

This is not a redesign.  
It is a precise layout / container / margin consistency fix for mobile.

## Core problem
On mobile, some card-based sections are going too edge-to-edge / full bleed compared to the rest of the portal layout.

They do not respect the same horizontal side margins / inner page gutters that other portal content uses.

Because of that, these sections feel visually broken and inconsistent.

## What must be fixed

### 1. Mis Paquetes — mobile card view
Inside Mis Paquetes, in the card view on mobile, each package card currently feels too full-bleed from left to right.

I want those package cards to have the same left and right page margin / gutter that the rest of the portal sections use.

### Important
I do not want:
- cards touching or nearly touching the viewport edges
- cards wider than the visual content grid
- random one-off padding hacks

I want:
- the package cards to align with the same mobile content width as the rest of the portal
- clean, intentional side breathing room
- consistent margin from the left and right edges

---

### 2. Dashboard cards / sections with the same issue
This same mobile spacing issue must also be fixed for these sections:

- Consolidación de Paquetes
- Notificaciones Importantes
- Consejos Rápidos

From the screenshots, these sections also look too edge-to-edge / inconsistent with the general mobile layout rhythm.

They should follow the same horizontal container logic / side margins as the rest of the portal.

---

## What I want visually
On mobile, all of these sections should feel like they belong to the same layout system:

- same outer side margins
- same container width logic
- same visual breathing room from viewport edges
- same card rhythm as the rest of the portal

The goal is not to make them narrower arbitrarily.  
The goal is to make them consistent with the rest of the page.

---

## Very important implementation note
Please do not solve this by adding random margins to one card instance only.

Instead:
- inspect the parent wrappers / mobile container classes / grid wrappers / section wrappers
- identify why these specific cards are escaping the expected mobile gutter
- fix the root layout cause
- make sure the spacing is consistent across the affected sections

---

## What to inspect
Please inspect:
- the mobile card layout in Mis Paquetes
- the wrappers of:
  - Consolidación de Paquetes
  - Notificaciones Importantes
  - Consejos Rápidos
- any section/container classes that are causing these blocks to render wider than the standard mobile content width
- any negative margins, width: 100vw, overflow tricks, or inconsistent padding wrappers

---

## Constraints
- do not redesign the cards
- do not change copy
- do not change information hierarchy
- do not change desktop layout unless required for consistency
- keep the current portal visual style
- this is a spacing / container consistency fix only

---

## Done looks like
- In Mis Paquetes mobile card view, package cards have the same left/right visual gutter as the rest of the portal
- Consolidación de Paquetes, Notificaciones Importantes, and Consejos Rápidos also respect the same mobile side margins
- No section feels full bleed unless that is explicitly intended elsewhere in the system
- Mobile layout feels cleaner, more premium, and more consistent
- No horizontal overflow is introduced
- Desktop remains unaffected or improved without regression

---

## Please report back with
1. what root layout/container issue was causing the full-bleed effect
2. what shared spacing/container fix you applied
3. confirmation that all four affected areas now follow the same mobile gutter logic
4. confirmation that desktop did not regress