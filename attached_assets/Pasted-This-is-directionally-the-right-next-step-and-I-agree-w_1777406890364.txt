This is directionally the right next step and I agree with adding the admin detail view plus AI confidence visibility for sales.

I want a few refinements before execution so the task stays clean, future-proof, and aligned with the broader quote workflow we discussed.

# Refinements required

## 1. Do not repurpose ai_extraction_id
Do not reuse ai_extraction_id to store the full JSON blob.

Instead:
- add a new nullable column such as ai_extraction_json
- store the full AIExtractionResult snapshot there
- keep ai_extraction_id semantically clean as an ID/reference field

This is important for long-term clarity and avoids mixing two different concepts.

## 2. Treat this as snapshot display and review only
This task should be:
- display
- review
- confidence visibility
- better admin decision-making

It should not add:
- re-extraction
- re-fetching
- re-running Gemini
- automatic correction of request fields
- any new AI decision layer in admin

Sales should see the AI snapshot exactly as it existed at submission time.

## 3. Add the system preliminary estimate to the admin detail view
One important addition from the broader workflow discussion:

If the request object already contains the system-generated preliminary estimate based on the existing calculator logic, the admin detail page should show it.

Add a dedicated section such as:

- Estimado del sistema
- total estimado
- servicio
- destino
- brief breakdown if available
- timestamp if available
- clear label/badge: Estimado automático

Copy guidance:
> “Este valor fue generado automáticamente usando la lógica actual de estimación de CRBOX. Es solo referencia interna y no sustituye la revisión comercial.”

This is important so sales is not starting from zero.  
They should be able to see:
- what the customer submitted
- what AI extracted
- what the current estimation engine produced

## 4. Improve clarity of data origin in the detail view
In addition to the overall data source badge (Manual, AI — parcial, AI — completo), the detail page should make it easy for sales to understand field origin without becoming visually noisy.

Recommended approach:
- keep the list view lightweight with only the overall source badge
- in the detail view, show per-field provenance/confidence only inside the AI section
- fields below 0.80 confidence highlighted in amber is correct
- missing fields shown as —
- do not overload the product summary block with too many micro-badges

## 5. Keep scope disciplined
Do not expand this task into:
- response composer
- package linking
- anomaly detection
- duplicate detection
- quote approval flow
- portal-native customer response rendering

Those remain later tasks.

# Expectations for the refined task
With the above refinements, the task should cover:

- admin list view gets a Ver link and a lightweight source badge
- admin detail page at /admin/solicitudes/<SCB-ID>
- customer block
- product block
- AI extraction snapshot block
- status history timeline
- status update form on the same page
- Estimado del sistema block if estimate data exists
- back link to the filtered list
- no regressions in current admin/list/status flows

# Please update the task spec accordingly
Please revise #185 with:
1. ai_extraction_json instead of repurposing ai_extraction_id
2. explicit “snapshot only” AI handling
3. the admin detail “Estimado del sistema” section
4. the clarified scope boundaries above

With those refinements, this looks like a strong next task.