Refine the current #113 plan with these final improvements before implementation:

* Clarify the duplicate-ID finding with more precision. Replace any wording that overstates certainty with this idea: the new site explicitly handles a backend duplicate-ID error response during registration; this confirms that IdentificationNumber is processed in the registration flow, but does not by itself prove that non-empty IdentificationNumber is required for a stable end-to-end new-site lifecycle.
* Keep the distinction extremely clear between:
    * what is confirmed directly from the new site
    * what is inferred from the legacy Vue flow
    * what is still only a hypothesis pending lifecycle validation
* In the implementation and final report, do not treat any legacy-derived requirement as fully confirmed unless it survives the real new-site lifecycle test.
* Keep the pre-implementation validation as the first priority. If the new site does not create accounts end-to-end successfully, stop the signup simplification work and stabilize the real registration lifecycle first.
* Keep the normal existing login flow untouched. Do not redesign, refactor, restyle, or alter login.html or its normal behavior unless there is a very specific technical blocker for post-registration auto-login. The only intended login-related change is the additive programmatic CRBOXAuth.doLogin(email, password) call right after successful registration.
* Strengthen the portal-side onboarding expectation: the destination inside Mi Cuenta must feel like a real activation/setup step, not just a generic settings page reached through a deep-link.
* At the same time, do not overdesign setup mode. Add this principle: setup mode should feel clearly guided and activation-oriented, but must remain visually coherent with the existing Mi Cuenta page and must not become a separate product surface or a one-off design language.
* Keep all setup-mode UX decisions aligned with the current CRBOX portal family: same design language, same interaction tone, same component logic, just more purposeful hierarchy and guidance.
* In the final report, explicitly explain:
    1. whether the new site currently creates accounts end-to-end,
    2. which “requirements” came from new-site validation vs. legacy inference,
    3. whether ID/address/phone were truly required for lifecycle stability,
    4. whether the lighter signup was safely adopted or had to be re-balanced,
    5. how Mi Cuenta setup mode was made guided without becoming visually disconnected from the rest of the portal.

The overall goal stays the same: a much better signup + activation-aware onboarding experience, but grounded in the real new-site lifecycle, not assumptions, and without breaking the login flow that already works well.