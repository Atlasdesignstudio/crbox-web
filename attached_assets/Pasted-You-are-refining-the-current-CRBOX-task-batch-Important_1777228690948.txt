You are refining the current CRBOX task batch.

Important strategic constraint:
We are NOT working on CRBOX’s internal operational tool yet.  
For this wave, focus only on:
- client portal improvements
- user-facing improvements inside the current portal
- anything that can be built truthfully from real existing client/account/package/factura data already available in the portal

This means:
- do NOT expand the batch into internal tools, ops dashboards, hidden admin pages, or staff-only utilities
- do NOT propose or keep standalone internal pages like ops.html in this implementation wave
- anything that belongs to an internal CRBOX operations layer should be explicitly moved out of scope for now

Current tasks to review:
- #119 EN ESPERA — Dashboard Separation
- #120 Journey & Branch Clarity — Mis Paquetes
- #121 Factura Billing Clarity
- #122 Package ↔ Factura Cross-Linking
- #123 MAWB Grouping — “Mismo Embarque”
- #124 Internal Stuck-Flow Detection — Ops View

Your role:
Critically refine this batch so it becomes a tighter, safer, more executable client-portal wave.

Core CRBOX rule:
Only show what is directly present, safely derived, or operationally confirmed.
Do not invent reasons, ETAs, promises, causes, discount semantics, readiness semantics, or status semantics we have not validated.

Important CRBOX context:
- The portal must remain truthful. No fake intelligence.
- EN ESPERA (status 6) must be visually/functionally distinct, but the reason is unknown from current data. Never imply blame, cause, or a specific reason.
- Status 5 must remain conservative unless business semantics confirm true pickup-readiness. Do not overstate “ya puedes retirarlo” if the data only proves arrival at CRBOX.
- Domicilio vs sucursal is real and should affect copy where operationally relevant.
- The package/factura relationship is real and valuable, but navigation must be grounded in what search/filter capabilities truly exist.
- MAWB is a real grouping signal, but user-facing wording must stay at “mismo embarque” / “mismo envío aéreo,” never “mismo vuelo.”
- Existing login flow must not be touched.
- UX should feel modern and more interpretive, but still native to the current CRBOX portal language and visual system.
- The goal is not “show more data,” but “help users understand what is happening, why one package differs from another, and what they can do next.”

What I want from you:
1. Review all six tasks critically.
2. Refine each one so it is:
   - truthfully grounded
   - minimal in scope
   - independently executable
   - not hiding dependencies
   - precise enough that an implementation agent can build it without guessing
3. Identify:
   - overlap
   - duplication
   - hidden coupling
   - unsafe wording
   - unconfirmed assumptions
   - tasks that should be split into MVP vs later phase
4. Tighten each task’s:
   - title
   - What & Why
   - Done looks like
   - Out of scope
   - Steps
5. Explicitly mark anything that still depends on business confirmation before shipping.
6. Preserve current portal visual coherence. No disconnected or overly “special” UX layer.
7. Do not redesign the whole portal. Refine only this task batch.

Critical instruction about #124:
Because we are not yet building CRBOX’s internal operational tool, #124 should not remain in this execution wave as an active implementation task unless you strongly argue for reducing it into something that is still strictly client-portal-facing.  
Default expectation: move #124 out of this batch and clearly explain why.

Refinement guidance by task

For #119 EN ESPERA — Dashboard Separation:
- Confirm this is framed as a truthfulness correction, not just a visual improvement.
- Recommend the single best UI pattern for the separate EN ESPERA signal (lightweight stat, inline notice, or contextual card). Choose one and explain why.
- Make sure status 6 is removed canonically from en-camino logic in one place only.
- Ensure status 6 copy stays neutral and does not imply cause or required action.
- Confirm whether the dashboard signal should say “comunícate con CRBOX” or something even more neutral.

For #120 Journey & Branch Clarity — Mis Paquetes:
- Tighten the stage explanation logic so it depends only on safe inputs: package status, known package branch field, and confirmed delivery mode.
- Review carefully whether “domicilio vs sucursal” should branch based on the user profile sucursal, the package-level branch, or a combination. If there is risk of mismatch, flag it clearly.
- Keep status 5 wording conservative unless true pickup-readiness is explicitly confirmed.
- Ensure consigneeSucursalName is only shown when actionable and non-empty.
- Strengthen the solution for the empty “Historial de Rastreo” section so it becomes honest and clean, not awkward.
- Make sure this task does not drift into factura logic, MAWB grouping logic, or dashboard logic.

For #121 Factura Billing Clarity:
- Confirm that the weight-driver treatment is safely derived and does not assume more than “compare the two displayed weights and tag the controlling one.”
- Keep the UI treatment subtle and native to the current factura table.
- Ensure the consolidation explanation states only factual grouping, never reasons or benefits.
- Review whether both row-level and header-level billing-weight tags should ship together or whether one should be MVP and the other follow later.
- Check if MAWB wording inside this task overlaps too much with #123.

For #122 Package ↔ Factura Cross-Linking:
- Review whether this should remain one task or split into:
  1) package → factura MVP link
  2) search/filter extension
  3) reverse factura → package navigation
- Validate actual current search behavior in Mis Facturas and Mis Paquetes before relying on it.
- If receipt-number search is not already real, do not let the task sound as though it is.
- Ensure the MVP path is clearly separated from the richer bidirectional path.
- Keep labels elegant and user-facing.
- Recommend the cleanest dependency ordering relative to #120 and #121.

For #123 MAWB Grouping — “Mismo Embarque”:
- Refine wording so all user-facing copy uses “mismo embarque” or “mismo envío aéreo,” never “mismo vuelo.”
- Review whether modal-level explanatory note and list/grid grouping should ship together or whether modal-first is the safer MVP.
- Ensure no claim overstates what shared MAWB means.
- Make sure list/grid grouping remains visually subtle and does not clutter the package list.
- Confirm the best sequence relative to #120 and #122.

For #124 Internal Stuck-Flow Detection — Ops View:
- Evaluate it explicitly against the strategic constraint that this wave is client-portal-only.
- Default recommendation should be to remove it from this batch and park it for a future internal-tools phase.
- If you think a small part of the thinking is still valuable now, extract only the portion that can safely improve the client portal without introducing an internal page, internal workflow, or staff-facing surface.
- Be strict: no hidden internal-tool scope should remain in this batch.

Cross-task refinement goals:
- Reduce overlap between #120, #121, #122, #123.
- Make dependencies explicit.
- Ensure each task has a clear MVP boundary.
- Prevent unconfirmed semantics from leaking into implementation.
- Keep all user-facing copy warm, clear, and natural in Spanish.
- Improve support deflection without fabricating explanations.
- Preserve portal design coherence.
- Keep this wave purely client-portal-facing.

Output format:
For each task (#119–#124), provide:
- Refined title
- Refined What & Why
- Refined Done looks like
- Refined Out of scope
- Refined Steps
- Risks / assumptions still needing confirmation
- Dependency / sequencing note
- MVP recommendation if relevant

Then add a final section:
“Recommended adjustments to the task set overall”

In that final section, state:
- which tasks should stay in this wave
- which should be split
- which should move earlier/later
- whether #124 should be removed from this wave
- any missing prerequisite tasks
- any wording conventions that should be standardized across the whole batch

Do not rewrite the whole CRBOX roadmap. Refine only this task set into a stronger client-portal execution batch.