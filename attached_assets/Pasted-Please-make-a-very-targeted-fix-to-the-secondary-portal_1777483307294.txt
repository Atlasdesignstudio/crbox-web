Please make a very targeted fix to the secondary portal navigation bar (the horizontal nav directly below the main top navbar, with: Dashboard, Mis Paquetes, Mis Facturas, Cotizaciones, Mi Cuenta).

This is not a redesign.  
It is a precise layout + selected-state correction.

## Problem 1 — the secondary nav is not truly centered
Right now, the whole second nav block keeps appearing visually aligned to the left.

I do not want it left-biased.
I want the entire secondary nav group itself to be horizontally centered in the viewport/page, so it feels balanced under the main navbar.

### Important
This does not mean:
- centering only the text inside each tab
- centering items within a left-aligned container
- adding random padding/margin that still leaves the whole group offset left

It means:
- the full nav pill/group must sit centered as a whole
- visually centered relative to the page width / viewport
- consistently centered across desktop widths

Please inspect the actual parent wrappers / flex containers / max-width containers and fix the root layout cause instead of applying a cosmetic offset.

## Problem 2 — the selected tab state looks wrong
The currently selected tab is looking more washed out / white / inactive than the non-selected tabs.

That is the opposite of what should happen.

The active tab must feel clearly selected.

### What I want for the active tab
The active tab should have a visibly stronger selected state, aligned with CRBOX branding.

Use a treatment such as:
- subtle orange emphasis
- orange text and/or icon
- light warm/orange-tinted background
- stronger contrast than inactive tabs
- clear selected feel without becoming visually aggressive

### What I do NOT want
- active tab looking faded, pale, disabled, or lower contrast than the rest
- active tab blending into the background
- a selected state that is less visible than hover

The selected state must be immediately understandable at a glance.

## UX intent
The second nav should feel:
- centered
- intentional
- balanced
- premium
- clearly interactive
- clearly indicate where the user currently is

## Constraints
- do not redesign the whole portal
- do not change the information architecture
- do not change labels
- do not change spacing drastically unless needed to fix centering/balance
- keep the general CRBOX visual system
- this is a layout and state refinement, not a new component concept

## Please do
1. Inspect the secondary nav container and all parent wrappers causing the left alignment
2. Make the entire nav group centered in the viewport
3. Fix the active/selected tab style so it is clearly highlighted
4. Ensure hover, inactive, and active states feel coherent together
5. Verify on desktop that the group is truly centered visually, not approximately
6. Keep it responsive and make sure mobile/tablet behavior does not break

## Report back with
- what root cause was making it stay left-aligned
- what container/layout fix you applied to truly center it
- what exact active-state styling you changed
- confirmation that the active tab is now more prominent than inactive tabs