I need a focused registration reliability + UI correction pass right now.

At this point I am worried about two things at the same time:

1. some field mappings may still be fragile or semantically off for what the backend historically expects, especially in Cuenta Empresarial
2. the current signup UI has real usability problems, especially on the final step where the primary CTA is not visually clear enough

This is not a general redesign request.
It is a backend-safety + conversion-critical UI fix pass.

⸻

1. Registration contract: what I need you to verify and protect

Do not get creative with field semantics.
Do not assume that because a key exists, the backend will happily accept any cleaner/newer interpretation of that key.

I want you to treat the historically validated behavior as the baseline of truth.

Personal account

Keep the personal flow aligned to the backend-safe structure we already validated:

* Consignee.ConsigneeName
* Consignee.ConsigneeLastName1
* Consignee.ConsigneeLastName2
* Consignee.Email
* ConfirmEmail
* Password
* ConfirmPassword
* Consignee.IdentificationType
* Consignee.IdentificationNumber
* Consignee.BirthDate
* Consignee.IsCompany = '0'
* Consignee.ResidenceCountry = 'CR'
* Consignee.Sucursal.IdSucursal
* structured Phones
* structured Addresses
* blank business-only fields:
    * Consignee.AlternativeEmail = ''
    * Consignee.ContactName1 = ''
    * Consignee.ContactName2 = ''
    * Consignee.IdResponsabilidad = ''
    * Consignee.Responsabilidad = '0'

Business account

Keep the business flow aligned to the historically validated working behavior:

* Consignee.ConsigneeName = company/business name
* Consignee.ConsigneeLastName1 = ''
* Consignee.ConsigneeLastName2 = ''
* Consignee.Email
* ConfirmEmail
* Password
* ConfirmPassword
* Consignee.IdentificationType = 'Otro'
* Consignee.IdentificationNumber
* Consignee.IsCompany = '1'
* Consignee.ResidenceCountry = 'CR'
* Consignee.Sucursal.IdSucursal
* structured Phones
* structured Addresses
* Consignee.ContactName1
* Consignee.ContactName2
* Consignee.AlternativeEmail
* Consignee.BirthDate = ''
* CompanyCode optional

⸻

2. Fields I specifically want you to treat as suspicious / high attention

I do not want a broad “looks fine” answer.
I want you to explicitly inspect these and either confirm them as safe or correct them.

Personal — inspect carefully

* Consignee.IdentificationType
* Consignee.IdentificationNumber
* Consignee.BirthDate
* ConsigneeLastName1
* ConsigneeLastName2
* barrio
* all province / canton / district derived values
* branch prefill values for sucursal mode

Business — inspect even more carefully

* Consignee.ContactName1
* Consignee.ContactName2
* Consignee.AlternativeEmail
* Consignee.IdentificationType
* Consignee.BirthDate
* Consignee.IsCompany
* ConsigneeLastName1
* ConsigneeLastName2
* barrio
* all province / canton / district derived values
* branch prefill values for sucursal mode

I am especially worried that the business flow may have been made semantically “cleaner” than what the backend historically expects. If that happened, bias back toward backend-safe behavior.

⸻

3. Costa Rica address logic: audit and protect it

I think the current implementation may have damaged or drifted from the old correct Costa Rica address logic.

Please explicitly audit and validate all of this:

* province options
* canton options
* district options
* cascading behavior between them
* whether old valid Costa Rica address combinations are still available
* whether valid selections are being blocked incorrectly
* whether branch prefill still resolves to the correct province / canton / district
* whether switching between sucursal and domicilio clears stale values correctly
* whether the final Addresses payload still reflects the correct Costa Rica location structure the backend expects

Do not just confirm final submit.
I want the address logic itself checked.

If any valid old address path is broken, missing, remapped incorrectly, or silently sending bad derived values, fix it.

⸻

4. UI corrections I need right now

The current UI has visible issues from a conversion / usability standpoint.

From the current screenshots, the most urgent issue is:

Critical issue

On the final step, the main submit CTA is not visually readable enough.
It appears to be present but visually disappears into the background or loses clear dominance.

That is unacceptable on a registration flow.
Fix this first.

UI issues to correct

Please correct these without breaking the backend behavior:

* make the final primary CTA clearly visible, dominant, and unmistakably actionable
* rebuild the bottom action zone so it feels intentional:
    * terms
    * error state
    * back button
    * primary submit CTA
* ensure the error message is visually attached to the failed action, not floating awkwardly
* reduce visual competition from non-essential elements near the bottom, especially promo/info blocks
* strengthen contrast between:
    * page background
    * form surface
    * inputs
    * selected delivery cards
    * primary action
* make the selected delivery card state clearer and more confidently “chosen”
* check whether step labels still match the actual content shown in each step
* keep the UI modern, clean, and CRBOX-consistent — but do not let aesthetics override clarity

⸻

5. Important constraint

Do not touch or redesign the normal existing login flow.
The regular login is already working.

The only allowed login-related behavior here is:

* verifying post-registration login works
* and preserving or improving the fallback/error handling around that

Do not redesign login.html.

⸻

6. What I want you to do next

Please do this in order:

A. Technical audit + correction

* inspect the suspicious fields listed above
* inspect address logic
* fix anything that is semantically risky or backend-fragile

B. Real live validation

Run one real end-to-end validation through the current new-site UI, especially for Cuenta Empresarial, using a real accepted email domain.

Capture and report:

* registration response
* auto-login result
* getuserinfo
* dashboard bootstrap
* Mi Cuenta bootstrap
* postedituser if possible

C. UI correction pass

After the contract/lifecycle is safe, fix the final-step CTA visibility and the bottom action zone issues.

⸻

7. Deliverable I want back

I want a precise report, not a vague reassurance.

Please return with:

1. Field-risk verdict
    * which fields were confirmed safe
    * which fields were suspicious
    * which fields were corrected
2. Costa Rica address verdict
    * whether the old correct location logic is still intact
    * what was broken if anything
    * what was fixed
3. Live lifecycle verdict
    * whether current Personal works
    * whether current Business works
    * exact failure mode if anything failed
4. UI correction verdict
    * what was wrong visually
    * what you changed
    * confirmation that the final CTA is now clearly visible and dominant

Main priority order:
backend correctness → address correctness → live lifecycle validation → UI clarity

Do not give me another broad theoretical answer. I need this checked, corrected, and validated concretely.