Thank you — that clarifies the sequencing.

So the picture is now clear:

- #192 is not built yet
- #193 is not built yet
- #194 is refined and ready, but should come after those two in the cleanest sequence

I agree with your recommendation on ordering.

## Approved sequencing
Please proceed in this order:

### 1. First — #193
Fix the broken Gemini model first.

Because:
- it is the smallest task
- it fixes a currently broken runtime feature
- it restores both extraction and draft assistance
- there is no reason to leave a live broken Gemini integration in place

### 2. Second — #192
Close the self-purchase / expected-package loop next.

Because:
- this is the first real write path for linked_package_id
- it makes the Lo compraré por mi cuenta branch operationally real
- it activates an already-built customer-side portal panel that is currently dead
- it closes the more structural gap before we expand the other branch

### 3. Third — #194
Then build the CRBOX-managed purchase state expansion.

That sequencing now makes sense:
- Gemini fixed
- self-purchase loop closed
- then CRBOX-managed branch expanded with manual operational progression

## I agree with the refined meaning of #194
The clarification on completada helps.

I understand it now as:
- comprado = CRBOX has already purchased the item
- completada = the CRBOX-managed purchase workflow is operationally closed from this system’s perspective
- and a later future state such as listo_para_retiro can be introduced separately when we want to represent Costa Rica arrival / pickup readiness more explicitly

That is a reasonable and truthful distinction for now.

## Please proceed like this
1. Turn #193 into the immediate task to tackle now  
2. After that, move directly to #192  
3. Keep #194 queued immediately after those two  
4. Preserve the current refined semantics and badge progression for #194 exactly as defined

## One additional requirement for #193
When implementing the Gemini fix, please do not just replace one hardcoded outdated model with another hardcoded model unless absolutely necessary.

If possible:
- make the Gemini model configurable via env var (for example GEMINI_MODEL)
- use a currently supported stable model for this project
- and report clearly which model is now being used for:
  - extraction
  - draft assistance

If that configurability is not practical right now, say so explicitly and document the exact hardcoded replacement model in the completion report.

## One additional requirement for #192
When implementing #192, please preserve the overlay principle explicitly:
- write linked_package_id only on quote_requests
- do not mutate any legacy CRBOX package backend records
- the portal linked-package panel should come alive only because the solicitud now contains real overlay metadata

## Final confirmation I want from you
Please confirm that you will now proceed in this order:

- #193
- #192
- #194

and then begin with #193 as the immediate next task.