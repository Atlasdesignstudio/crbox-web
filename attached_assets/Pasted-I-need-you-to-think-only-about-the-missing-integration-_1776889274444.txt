I need you to think only about the missing integration context from the legacy CRBOX system.

You already know the new website, its current structure, and the UI/screens that exist in this Replit project, so do not spend time re-describing the frontend. Instead, focus on what you would need from the legacy CRBOX system in order to make the current login and signup pages fully functional against the existing backend/database.

Legacy CRBOX context you should work from:
- CRBOX currently appears to run on a legacy multi-layer architecture.
- There is a public site layer.
- There is a client/account area.
- There is an admin/internal area.
- There is a backend/API layer.
- There is a shared database behind those systems.
- The infrastructure appears to include older server-based components and AWS is also part of the current stack/environment.
- The current system should be treated as an existing live legacy backend that already handles customer accounts, authentication, and persistence.
- The goal is NOT to replace that backend right now.
- The goal is to connect the new login and signup UI to the current legacy backend safely.

Important assumptions/constraints:
- Signup has two modes: personal and business.
- Login must authenticate against the current legacy system.
- We do not want to write directly to database tables from the new frontend.
- We want to understand the integration contract the legacy system requires.
- Any mention of AWS should be treated as part of the current legacy environment/infrastructure context, not as something to redesign right now.

What I need from you:
Based on the UI/screens that already exist in this project, tell me exactly what the new site would need from the legacy engineer/backend side in order to connect those pages to the current system.

Please structure your answer like this:

1. LEGACY SYSTEM SUMMARY
- Summarize how CRBOX is likely structured based on the legacy context:
  - public site
  - client/account system
  - admin/internal system
  - backend/API
  - shared database
  - AWS/infrastructure layer
- Keep it concise and practical.

2. WHAT THE LEGACY SYSTEM PROBABLY HANDLES
- Login/authentication
- Signup/account creation
- Personal vs business account logic
- Validation
- Session/cookies/tokens
- Catalogs/dropdown values
- Any automatic side effects like customer creation, locker assignment, address creation, emails, etc.

3. WHAT IS MISSING FOR INTEGRATION
List exactly what is still unknown and must be confirmed from the legacy side.

4. EXACT BACKEND INFO TO REQUEST
Give me a precise checklist of what I need to ask the engineer for, including:
- login endpoint
- signup endpoint(s)
- request format
- required fields
- hidden/internal fields
- validation rules
- success/error responses
- auth/session behavior
- password reset flow
- catalog values
- CORS / domain / cookie requirements
- staging or test method

5. FIELD MAPPING APPROACH
Using the forms/screens already present in this project, explain how to think about mapping:
- UI labels
- frontend field keys
- backend field keys
- personal vs business variants
- optional vs required fields

Do not invent exact backend keys unless clearly marked as assumptions.

6. INTEGRATION OPTIONS
Compare:
- redirecting to the old pages
- connecting the new UI directly to legacy endpoints
- using a middleware/backend bridge between the new UI and the legacy system

For each one, explain:
- what is needed
- risks
- pros/cons
- which one is safest first

7. SAFEST PLAN
Tell me the safest practical phased plan to make the current login/signup screens functional without risking the current system.

8. WHAT YOU CAN PREPARE NOW IN THIS REPLIT PROJECT
Be very concrete about what can already be built safely right now before having backend details.
Examples:
- validation schemas
- typed payload models
- API service layer placeholders
- loading/error states
- success/error handling
- environment variable structure
- integration toggles
- manual test mode
- mock adapters

9. WHAT SHOULD WAIT
Be strict about what should not be implemented yet until the engineer confirms the backend contract.

10. BEST FORMAT TO RECEIVE FROM THE ENGINEER
Tell me the best formats for the engineer to hand over the integration details so another engineer or AI agent can implement them easily.
Examples:
- OpenAPI/Swagger
- Postman collection
- request/response JSON examples
- field dictionary
- endpoint documentation

Important constraints:
- Do not assume the legacy endpoint names are known.
- Do not assume the legacy database schema should be touched directly.
- Be explicit about assumptions.
- Focus on practical implementation, not theory.
- The final goal is to make the existing login/signup UI in this project work against the current legacy CRBOX backend safely.