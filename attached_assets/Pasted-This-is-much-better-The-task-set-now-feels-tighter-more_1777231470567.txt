This is much better. The task set now feels tighter, more honest, and much more activation-ready.

A few notes on the outcome:

- #127 is now in the right shape: a technical prerequisite, not a user-facing feature. That is exactly how it should be framed.
- #86 improved significantly. The discovery that search/date filtering are already wired and that the real remaining gap is the dead Enter-key submit handler is important. That makes it a small, real cleanup task rather than a misleading “feature build.”
- #125 now has a clear enough UX pattern to activate. The decision to show EN ESPERA packages in a separate neutral section at the top, while ensuring each package appears only once, is the right direction.
- #126 is now properly framed as a Phase 2 follow-up, with a more conservative MVP. Good call not to overcommit to automatic modal-open behavior yet.
- #85 is correctly parked and should stay blocked until real backend event history exists.

One final refinement I want you to add before activation:

## Additional refinement for #126
Please make the dependency on actual search capability even more explicit.

Right now, #126 correctly calls out that reverse navigation depends on validating whether getuserpackages accepts receipt numbers in the tracking/search parameter. I want the task to state this more strongly:

- if getuserpackages does support searching by receipt number, then the MVP can be:
  - navigate to mis-paquetes.html?receipt=<number>
  - prefill the search
  - show the matching package in the list

- if getuserpackages does not support receipt-number search, then the task must not pretend that server-side reverse navigation exists.
  In that case, the task should either:
  - fall back to a clearly defined client-side filter over already loaded packages, if that is truly reliable and safe
  - or stop and report that reverse navigation needs additional search capability before it can ship truthfully

In other words:
#126 must not quietly assume backend/package-search support that has not yet been validated.

Please update #126 with that extra explicit decision branch.

## Activation order
Assuming that refinement is added, this is the order I agree with:

1. #127 — activate first  
2. #86 — activate now in parallel with #127  
3. #125 — activate after that  
4. #126 — only after #127 confirms the join key and after search behavior is explicitly validated  
5. #85 — keep parked / blocked

Overall, this batch is now in good shape. Add that final strengthening to #126, then I’m comfortable with this next-wave set.