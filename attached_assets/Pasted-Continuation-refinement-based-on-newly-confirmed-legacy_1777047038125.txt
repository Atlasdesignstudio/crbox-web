Continuation refinement based on newly confirmed legacy API inspection:

We found additional confirmed real response details from the legacy CRBOX portal that must now be used as source of truth for the new portal implementation. This is not a redesign request. It is a continuation parity/truthfulness refinement so the new portal stops under-leveraging real backend data.

## Newly confirmed from legacy getuserpackages
In addition to the package fields already known, the legacy response inspection confirms these fields are real and available per package row and should be considered in the package renderer and truthfulness audit:

- totalvolume
- consigneeSucursalName
- descripcionfactura
- montofactura
- invoicesCount

The expanded confirmed package row now includes:
- idwarehousereceipt
- statusId
- statusName
- number
- receiveddatetime
- createdDate
- trackingNumber
- shipperName
- totalpieces
- totalweight
- totalvolume
- totalvolumetricweight
- carrierName
- consigneeNotes
- consigneeSucursalId
- consigneeSucursalName
- masterAirShipmentNumber
- airShipmentNumber
- descripcion
- descripcionfactura
- montofactura
- invoicesCount
- hasPackage
- impresoFactura
- consolidadoFactura
- emision

### What to do with this
- totalvolume should be included wherever dimensional/shipping metadata is shown, especially in package detail surfaces.
- consigneeSucursalName should be considered as useful contextual metadata in package detail.
- descripcionfactura, montofactura, and invoicesCount must now be explicitly audited as package-adjacent billing fields:
  - if their user-facing meaning is defensible from the response alone, surface them in the most appropriate detail-level UI;
  - if their semantics are ambiguous or would mislead the user, keep them hidden or neutral and document why.
- Do not ignore these newly confirmed real fields during the package audit.

## Newly confirmed from legacy getfacturas
We also confirmed from the old working portal that the new Mis Facturas page is not failing because of the backend. The real getfacturas request fires, returns 200 OK, and returns a non-empty array. So this is a renderer/mapping problem, not an API/fetch problem.

Confirmed real bills response shape:
- top-level response is an array
- each row is an object with:
  - Factura
  - Recibos

### Confirmed Factura fields
- Factura.factura
- Factura.masterAirShipment.masterairshipmentnumber
- Factura.weigth
- Factura.volumentricWeigth
- Factura.cantidadBultos
- Factura.total
- Factura.descuentoCorporativo._nombre
- Factura.isInvoiced
- Factura.createdDate
- Factura.billedDate

### Confirmed Recibos[] fields
- number
- receiveddatetime
- totalweight
- totalvolume
- totalvolumetricweight
- status.statusname
- shipper.shippername
- carrierinformation.carrier.carriername

## Critical correction for Mis Facturas
Please treat Mis Facturas as a confirmed render-shape issue:
- the request is firing,
- auth works,
- response is non-empty,
- the page must be rewritten against the real Factura + Recibos[] shape.

Do not keep debugging this as a network/auth problem unless you find evidence of a new regression.

## Continuation tasks required now

### 1. Refine Mis Paquetes with the stronger confirmed shape
Update the package normalization/helper so it also includes:
- totalvolume
- consigneeSucursalName
- descripcionfactura
- montofactura
- invoicesCount

Then re-audit the following:
- list/table columns
- grid card metadata
- detail modal
- tracking modal
- package-adjacent informational blocks

Do not force all fields into overview. Place them where they provide the most user value without clutter.

### 2. Fix Mis Facturas renderer against the real legacy shape
Audit the current Mis Facturas implementation and identify:
- which wrong/guessed field names it still expects,
- where it assumes a flat bill object instead of row.Factura,
- whether it is mishandling Recibos[],
- whether null dates (billedDate, createdDate) are breaking formatting or row rendering.

Then replace the bill mapping so the main bills table renders directly from:
- row.Factura.factura
- row.Factura.masterAirShipment.masterairshipmentnumber
- row.Factura.weigth
- row.Factura.volumentricWeigth
- row.Factura.cantidadBultos
- row.Factura.total
- row.Factura.descuentoCorporativo._nombre
- row.Factura.billedDate
- fallback to row.Factura.createdDate only if billedDate is empty

And use row.Recibos[] for any receipt-level detail surface or joined receipt display.

### 3. Keep truthfulness rules intact
Do not invent payment status, paid/unpaid labels, or invoice lifecycle semantics unless explicitly supported by the backend shape.
Do not fabricate package-adjacent billing signals from descripcionfactura, montofactura, or invoicesCount unless their meaning is defensible.
If a field is real but ambiguous, keep it neutral and document why.

### 4. Re-evaluate dashboard derivations
Because the package shape is now stronger, re-check whether any dashboard derivations can be made more accurate using:
- explicit statusId / statusName
- receiveddatetime
- createdDate
- package-adjacent invoice fields only if semantics are defensible

Do not add anything fake to fill visual space.

## Deliverable for this continuation
At the end of this refinement, report back with:
1. which newly confirmed package fields were now surfaced,
2. which newly confirmed package fields were intentionally not surfaced and why,
3. which wrong assumptions in Mis Facturas were removed,
4. whether Mis Facturas is now rendering real rows from the confirmed Factura + Recibos[] shape,
5. any remaining limitations caused by ambiguous backend semantics rather than UI implementation.

Preserve current design and layout. This is a continuation parity/truthfulness refinement based on newly confirmed legacy inspection, not a redesign.