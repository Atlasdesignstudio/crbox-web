I need you to stop assuming the current signup implementation is correct and do a focused registration recovery + UX/contract alignment pass for both Cuenta Personal and Cuenta Empresarial.

Right now I am not confident that:
1. the current UI is the best version,
2. the fields are being collected in the right way,
3. the payload semantics still match what the backend actually expects,
4. or that the current business flow is reliably working.

So this is the task:

## Goal
Make sure both signup flows:
- are actually working against the real backend
- collect the right fields in the right way
- send a payload that matches backend expectations semantically, not just by key name
- and present a UX that is clear, modern, and appropriate for what the backend really needs

This is not a cosmetic pass only.  
It is a backend contract alignment + UX correction task.

---

## What I want you to do

### 1. Re-validate both flows through real end-to-end testing
Do not rely on implementation snapshots alone.

Test both:
- Cuenta Personal
- Cuenta Empresarial

For each one, validate the full chain:
- register
- auto-login or manual login
- getuserinfo
- dashboard bootstrap
- Mi Cuenta load
- postedituser if possible

Use real accepted email domains, not throwaway domains.

If either flow does not work end-to-end right now, treat that as a real failure and fix it before doing any UX polishing.

---

### 2. Compare current payload semantics against the validated working behavior
I am especially worried that the implementation may have become too “creative” with how it maps fields, especially in Cuenta Empresarial.

Do not only check whether the same keys exist.  
Check whether the backend is likely receiving the values in the way it historically expects.

For both flows, inspect the current real payload and compare it against the previously validated working behavior.

Pay special attention to these fields:

#### Personal
- Consignee.ConsigneeName
- Consignee.ConsigneeLastName1
- Consignee.ConsigneeLastName2
- Consignee.BirthDate
- Consignee.IdentificationType
- Consignee.IdentificationNumber
- Consignee.Sucursal.IdSucursal
- Phones
- Addresses

#### Empresarial
- Consignee.ConsigneeName
- Consignee.ConsigneeLastName1
- Consignee.ConsigneeLastName2
- Consignee.IsCompany
- Consignee.IdentificationType
- Consignee.IdentificationNumber
- Consignee.BirthDate
- Consignee.ContactName1
- Consignee.ContactName2
- Consignee.AlternativeEmail
- Consignee.Sucursal.IdSucursal
- Phones
- Addresses
- CompanyCode

I want you to detect whether the current implementation is:
- semantically aligned,
- suspicious,
- or likely mismapped.

---

### 3. If needed, correct the flows to match backend expectations better
If you find that the current UI/payload mapping is too clever, too transformed, or too far from what the backend historically accepts reliably, then simplify/correct it.

Priority is:
- backend reliability first
- good UX second
- but ideally both

Do not preserve a clever modernized mapping if it makes backend behavior more fragile.

---

### 4. Improve the UX only after the payload/lifecycle is trustworthy
Once the flows are truly working, refine the UX so it feels intentional and modern.

That means:
- cleaner grouping
- less visual density
- stronger hierarchy
- better step-by-step guidance
- mobile-friendly behavior
- clearer field labels
- clearer difference between personal and business account

But:
- do not invent perks or benefits not confirmed by CRBOX
- do not force a lighter flow if the backend needs more data
- do not break the existing working login flow

---

## Important product guidance for Cuenta Empresarial
CRBOX does not clearly communicate special business benefits, so do not invent any.

The business mode should be made clear through:
- naming
- field structure
- account-type labeling
- business-specific contacts/emails/identification
- truthful explanation that this is a company/business profile

Do not sell fake advantages like better pricing, tax handling, priority support, etc. unless they are explicitly confirmed.

But do make the mode legible and intentional.

---

## Specific concerns I want you to resolve
Please specifically check and resolve these:

- Is Cuenta Personal currently still producing a stable, correct account end-to-end?
- Is Cuenta Empresarial currently still producing a stable, correct account end-to-end?
- Has any field mapping been changed in a way that no longer matches the backend’s historical expectations?
- Is the empresarial flow currently too “cleaned up” semantically, especially around:
  - ContactName1
  - ContactName2
  - AlternativeEmail
  - surname handling
  - birth date handling
- Is the current UI asking for the right fields in the right place?
- Is the current stepper structure actually the best UX for what the backend needs?
- Are sucursal vs domicilio paths behaving correctly and producing the right address payload?

---

## Constraints
- Do not redesign or alter the normal existing login flow.
- Do not assume legacy UX should be copied.
- Do not assume legacy payload semantics can be changed freely just because the keys still exist.
- Do not leave this as a theory exercise — validate with real requests and real outcomes.
- Do not prioritize prettier UX over a stable real registration lifecycle.

---

## Deliverable I want back
I want you to return with:

1. Current status of Cuenta Personal
   - working or broken
   - what was validated
   - what was fixed if needed

2. Current status of Cuenta Empresarial
   - working or broken
   - what was validated
   - what was fixed if needed

3. Payload alignment verdict
   - which fields are correctly aligned
   - which fields were suspicious
   - which fields were changed to better match backend expectations

4. UX verdict
   - whether the current UI was good enough
   - what was improved
   - why those improvements better match the real lifecycle

5. Final result
   - both flows technically correct
   - both flows UX-improved
   - or what remains blocked

The main thing I want is this:
make both signup flows truly reliable and aligned with backend reality, and then make them feel as good as possible without inventing or breaking anything.