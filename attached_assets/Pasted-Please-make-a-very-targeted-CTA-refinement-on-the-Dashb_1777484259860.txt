Please make a very targeted CTA refinement on the Dashboard of the client portal.

This is not a redesign.  
It is a precise fix for the CTAs in two specific places:

1. the “Nueva solicitud” button inside “Solicitar compra con CRBOX”
2. the left CTA under “¡Bienvenido, Juan Carlos!”

---

## 1. Fix “Nueva solicitud” visibility
Inside the dashboard module:

- Solicitar compra con CRBOX

there is a button for:
- Nueva solicitud

Right now it looks white on white / washed out / almost invisible.

It is technically there, but it does not read as a CTA.

### What I want
Make “Nueva solicitud” a clearly visible orange CTA, aligned with the CRBOX primary action style.

It should feel:
- intentional
- visible at first glance
- clearly clickable
- available now
- not disabled
- consistent with CRBOX orange actions elsewhere in the site

### Please check
- whether the button is accidentally inheriting disabled styles
- whether opacity is being reduced
- whether text and background lack contrast
- whether a neutral/shared button class is overriding the intended CTA treatment

If needed, define a dedicated CTA class and apply it explicitly.

---

## 2. Update the hero CTA under “¡Bienvenido, Juan Carlos!”
Inside the dashboard hero, below:

- ¡Bienvenido, Juan Carlos!

there is currently a CTA that says:
- Calcular Envío

Sometimes this should not be Calcular Envío there.

### What I want
Change that CTA to:

- Cotizar

and make it take the user directly to the Cotizaciones tab/section inside the client portal.

### Important
This should behave as a portal-navigation CTA, not as a public-site calculator CTA.

So:
- label = Cotizar
- destination = the portal Cotizaciones area/tab
- not the public calculator page

If the cleanest implementation is linking directly to the Cotizaciones page/tab route, do that.  
If it needs to trigger the existing internal portal navigation, do that.  
Use the most robust implementation, not the most superficial one.

---

## UX intent
The dashboard should communicate:
- Nueva solicitud = the obvious orange action to create a new request/quote
- Cotizar = the hero CTA that takes the user into the portal quoting flow directly

These should not feel ambiguous or washed out.

---

## Constraints
- do not redesign the whole dashboard
- do not change surrounding copy unnecessarily
- keep CRBOX palette
- keep the overall module structure
- make both CTAs feel intentional and consistent with the portal

---

## Done looks like
- Nueva solicitud is clearly visible as an orange CTA
- it no longer looks white-on-white, disabled, or broken
- the hero CTA now says Cotizar
- clicking Cotizar takes the user to the portal Cotizaciones tab/section
- both CTAs feel coherent with the CRBOX portal style

---

## Please report back with
1. what was causing the invisible/washed-out “Nueva solicitud” state
2. what exact style/class fix was applied
3. how the hero CTA was changed from Calcular Envío to Cotizar
4. what exact destination/route/action now opens the portal Cotizaciones area