Please make a targeted mobile-responsive fix for the section on the Calculadora page titled:

- Elige el mejor servicio
- Comparativa de Servicios

This task is only about making this section work properly on mobile.  
Do not redesign the whole page.  
Do not change the desktop experience unless a tiny adjustment is required for consistency.

## Problem
Right now, on mobile, this comparison section is clearly breaking:

- content is overflowing
- columns are cramped
- headings and descriptions collide
- rows do not read cleanly
- CTAs do not feel mobile-native
- the whole module feels like a desktop comparison table being squeezed into a phone screen

The result is not usable enough for mobile.

---

## Main objective
Make this section feel mobile-native, while preserving the comparison logic and the CRBOX visual language.

That means:
- readable
- tappable
- structured clearly
- no overlap
- no clipped text
- no broken columns
- CTA actions easy to use on mobile

---

## What to fix specifically

### 1. Make the comparison layout mobile-native
The current desktop/table-style comparison should not simply shrink on mobile.

Please adapt the mobile layout so the section becomes naturally readable on a phone.

Use the best solution, for example:
- stacked comparison cards
- horizontal snap/scroll panels
- service tabs with one service shown at a time
- a vertically structured comparison block

I do not want a broken mini-table squeezed into mobile.

The section should feel intentionally designed for mobile.

---

### 2. Preserve comparison clarity
The user still needs to understand clearly the difference between:

- Servicio Aéreo
- Servicio Marítimo

So the mobile version must still communicate:
- delivery time
- ideal weight/use case
- relative cost
- CTA/action

But it should do so in a way that is clean on small screens.

---

### 3. Make CTAs mobile-native
The CTA area inside this section must also work properly on mobile.

Buttons like:
- Calcular aéreo
- Cotizar marítimo

should:
- be easy to tap
- not feel cramped
- not overlap
- not look like squeezed desktop buttons
- have proper spacing
- feel intentional on mobile

If needed, stack them or restructure them for mobile.

---

### 4. Make any tabs / service-switching behavior feel good on mobile
If there are tabs, segmented controls, or switching logic in this section, make sure they feel mobile-native too.

That means:
- proper tap targets
- no overflow issues
- clean spacing
- clear active state
- visually obvious navigation

If the current tab logic is not suitable for mobile, adapt it.

---

### 5. Typography and spacing
Please review:
- title spacing
- subtitle line length
- comparison label sizes
- row spacing
- internal card padding
- CTA spacing
- section breathing room

This section should feel composed and legible on mobile, not compressed.

---

## Constraints
- focus only on this section
- preserve CRBOX palette and overall style
- do not redesign desktop unnecessarily
- do not break any existing CTA destinations or logic
- do not just add horizontal overflow and call it done unless it is genuinely elegant and usable
- priority is a true mobile-native experience

---

## Done looks like
- the Comparativa de Servicios section works properly on mobile
- there is no overlapping or broken layout
- the content is easy to compare on a phone
- CTAs are clearly tappable and well spaced
- tabs/service navigation, if present, feels mobile-native
- the section feels intentionally responsive, not just shrunk down

---

## Please report back with
1. what mobile layout approach you used for the comparison
2. how the Aéreo vs Marítimo comparison is now presented on mobile
3. what was changed for the CTA area
4. confirmation that desktop behavior was preserved or only minimally adjusted